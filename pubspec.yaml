name: jackshub
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore:
  firebase_auth: ^0.15.0+1
  cupertino_icons: ^0.1.2
  fluro: "^1.4.0"
  auto_size_text: ^2.1.0
  sqflite:
  path_provider:
  flutter_bloc:
  equatable:
  provider: ^3.2.0
  flutter_spinkit: "^4.1.0"


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: "^0.7.0"

flutter:
  uses-material-design: true
  fonts:
    - family: Hobo
      fonts:
        - asset: assets/fonts/HoboBT.ttf
  assets:
    - assets/images/
